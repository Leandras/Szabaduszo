{% extends 'master' %}

{% block title %}Kezdőlap{% endblock %}
{% block content %}
<!-- <form action="{{ 'job_list' | route }}" method="GET">
    <div class="input-group search-box">
      <input type="text" class="form-control" placeholder="Keresés..." name="jobName">
      <span class="input-group-btn">
        <button class="btn btn-info" type="submit">Mehet!</button>
      </span>
    </div>
  </form>
 -->
 <!-- {% for job in jobs %}
  <div class="list-group">
  
   <p>{{ job.name }}</p> 
  </div>
{% endfor %}
-->
<!--
 {% for cat in category %}
  <div class="list-group">
   <p>{{ cat.name }}</p> 
  </div>
{% endfor %}
-->
<!--
 <ul class="nav nav-tabs">
    {% for categoryBatch in categories | batch(3) %}
      {% for category in categoryBatch %}
        {% if(category.id == 1) %}
          <li class="active"> <a href="{{ 'job_list' | route({ id: category.id }) }}?category={{category.id}}">
          {{ category.name }}</a></li>
        {% else %}
        <li role="presentation"><a href="{{ 'job_list' | route({ id: category.id }) }}?category={{category.id}}">
          {{ category.name }}</a>
        </li>
        {% endif %}
      {% endfor %}
    {% endfor %}
  </ul>
  <div class="panel panel-default">
    <div calss="panel-heading">
      <p>Heading, tábla majd ide</p>
    </div>
    <div class="list-group">
       {% for job in category.latestJobs %}
         <a href="{{ 'job_page' | route({id: job.id}) }}" class="list-group-item">{{ job.name }}</a>
       {% endfor %}
    </div>
  </div>
  -->
 <!-- {% for categoryBatch in categories | batch(3) %}
  <div class="row">
    {% for category in categoryBatch %}
    <div class="col-md-4">
      <div class="panel panel-default">
        <div class="panel-heading">
          {{ category.name }}
          <a href="{{ 'job_list' | route }}?category={{ category.id }}" class="btn btn-warning btn-xs pull-right">Összes megtekintése</a>
        </div>
        <div class="list-group">
          {% for job in category.latestJobs %}
          <a href="{{ 'job_page' | route({id: job.id}) }}" class="list-group-item">{{ job.name }}</a>
          {% endfor %}
        </div>
      </div>
    </div>
    {% endfor %}
  </div>
  {% endfor %}
  -->
{% endblock %} 
